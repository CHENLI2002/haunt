// Landing — the v0 validation surface (find/browse a venue → follow → feed).
// See plan §8 (MVP scope) and §12 (impact-ordered backlog, items #1–#9).

export default function HomePage() {
  return (
    <main>
      <h1>Haunt</h1>
      <p>Follow a venue. See who plays next.</p>
      {/* TODO v0: venue search/browse + follow + follow-feed */}
    </main>
  );
}
